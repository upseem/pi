# 本仓库怎么用

本机已经把 `pi` 做成全局命令，指向这个仓库的源码（`pi-test.sh`）。改 `packages/` 后直接再跑 `pi` 即可，不必重新编译。

先登录，再用 **coding-agent**（日常终端里的 `pi`）。以后要自己写程序嵌代理，再用 **agent**（`@earendil-works/pi-agent-core`）。

## 1. 先登录

在任意目录启动交互模式：

```bash
pi
```

进去后输入：

```text
/login
```

按提示选提供商。常见选择：

| 方式 | 适用 |
|------|------|
| Claude Pro/Max | Anthropic 订阅 |
| ChatGPT Plus/Pro (Codex) | OpenAI 订阅 |
| GitHub Copilot | Copilot 订阅 |
| OpenRouter / xAI / Radius | 对应订阅或 OAuth |
| API-key 提供商 | 把 key 存进 `~/.pi/agent/auth.json` |

不想走 `/login` 时，也可以先设环境变量再启动：

```bash
export ANTHROPIC_API_KEY=sk-ant-...
pi
```

凭据在 `~/.pi/agent/auth.json`。退出登录用 `/logout`。

登录成功只表示凭据写好了，**当前模型不一定会切过去**。环境里如果已有 `GOOGLE_CLOUD_PROJECT` 等，启动时可能先选中 Google Vertex。发消息前先切模型：

```text
/model
```

选 `openai-codex`（或你刚登录的提供商），需要的话在选择器里按 Ctrl+S 存成默认。也可以启动时指定：

```bash
pi --provider openai-codex
```

检查某个提供商是否就绪：

```bash
pi auth check --provider anthropic
```

完整提供商列表见 [packages/coding-agent/docs/providers.md](packages/coding-agent/docs/providers.md)。

## 2. 日常：coding-agent（就是 `pi`）

`@earendil-works/pi-coding-agent` 是带工具的编码代理 CLI。它会读、改当前目录里的文件，并跑 bash。没有内置沙箱，权限等于启动它的用户。

### 交互

```bash
cd /path/to/your-project
pi
```

输入任务，Enter 发送。默认可用工具：`read`、`write`、`edit`、`bash`。`find` / `grep` 依赖本机的 `fd` 和 `rg`，需要自己装，例如 `brew install fd ripgrep`。pi 不会从 GitHub 下载。

常用操作：

| 操作 | 怎么做 |
|------|--------|
| 引用文件 | 编辑器里输入 `@`，或 `pi @README.md "总结一下"` |
| 跑一条本地命令 | `!npm run check`（`!!cmd` 跑命令但不把输出给模型） |
| 换模型 | `/model` 或 Ctrl+L；Ctrl+S 保存为默认 |
| 换思考级别 | `/thinking` 或 Shift+Tab |
| 换界面语言 | `/lang`，或启动时 `pi --lang zh-CN` / `pi --lang en`（`auto` 跟系统语言） |
| 新会话 | `/new` |
| 继续上次 | `pi -c` |
| 选历史会话 | `pi -r` |

给项目写说明，放 `AGENTS.md`。改完后 `/reload`。

### 非交互（脚本、一次性任务）

```bash
pi -p "Summarize this repository"
pi --provider anthropic --model claude-sonnet-4-6 -p "Review src/"
```

帮助：

```bash
pi -h
pi --lang en -h    # 英文帮助
pi --lang zh-CN -h # 中文帮助
```

未指定 `--lang` 时，按设置里的 `language`（`/lang` 可持久化）或系统语言自动选择。

### 本仓库调试

全局 `pi` 用的是源码。改 CLI / 工具 / TUI 后，新开一次 `pi` 就能看到效果。

只有需要独立 `dist` 产物时才编译：

```bash
cd /Users/chen/www/pi
npm run build:offline
```

## 3. 后期：agent（嵌进自己的程序）

`@earendil-works/pi-agent-core` 不是另一个 CLI，而是代理运行时库：消息循环、工具执行、事件流。coding-agent 内部也用它。

适合：自己做 TUI、服务端、脚本，只要循环和工具，不要整套 `pi` 交互界面。

最小例子（登录后的凭据仍走 `pi-ai` 的提供商）：

```typescript
import { Agent } from "@earendil-works/pi-agent-core";
import { createModels } from "@earendil-works/pi-ai";
import { anthropicProvider } from "@earendil-works/pi-ai/providers/anthropic";

const models = createModels();
models.setProvider(anthropicProvider());
const model = models.getModel("anthropic", "claude-sonnet-4-6");
if (!model) throw new Error("Model not found");

const agent = new Agent({
  initialState: {
    systemPrompt: "You are a helpful assistant.",
    model,
  },
  streamFn: models.streamSimple.bind(models),
});

agent.subscribe((event) => {
  if (event.type === "message_update" && event.assistantMessageEvent.type === "text_delta") {
    process.stdout.write(event.assistantMessageEvent.delta);
  }
});

await agent.prompt("Hello!");
```

在本仓库里可以直接引用 workspace 包。更完整的 API 见 [packages/agent/README.md](packages/agent/README.md)。

coding-agent 的编程入口是 SDK（`createAgentSession()`），带会话、扩展、内置工具：

```bash
# 示例
packages/coding-agent/examples/sdk/
```

文档：[packages/coding-agent/docs/sdk.md](packages/coding-agent/docs/sdk.md)。

## 4. 怎么选

| 你想做的事 | 用什么 |
|------------|--------|
| 在终端里改代码、跑命令 | `pi`（coding-agent） |
| 把「模型 + 工具循环」嵌进自己的程序 | `pi-agent-core` |
| 统一调各家 LLM，不管工具循环 | `pi-ai` |
| 自己画终端 UI | `pi-tui` |

建议顺序：先 `/login`，用 `pi` 跑几天；需要自己写宿主程序时再接 agent 或 SDK。

## 5. 同步 upstream

本 fork 用 `upstream` 跟踪官方仓库。合并上游前先保证**已跟踪文件干净**（未跟踪文件可忽略；若 `Mac快捷键.md` 有本地改动，先自行处理）。

```bash
cd /Users/chen/www/pi
./scripts/sync-upstream.sh
# 或: npm run sync:upstream
```

脚本会：`git fetch upstream` → `git merge --no-commit --no-ff upstream/main` → 冲突则打印冲突文件并停止（不自动解决、不 push）→ 成功则 `npm install --ignore-scripts`、`npm run generate:models`、`npm run check`。

检查通过后**自行审查并手动 commit**。默认不 commit、不 push。需要脚本代为提交时：

```bash
./scripts/sync-upstream.sh --commit
```

## 6. 相关文档

- [packages/coding-agent/docs/quickstart.md](packages/coding-agent/docs/quickstart.md) — 首次会话
- [packages/coding-agent/docs/usage.md](packages/coding-agent/docs/usage.md) — 交互、斜杠命令、CLI
- [packages/coding-agent/docs/keybindings.md](packages/coding-agent/docs/keybindings.md) — 快捷键
- [packages/coding-agent/docs/sessions.md](packages/coding-agent/docs/sessions.md) — 会话
- [SECURITY.md](SECURITY.md) — 信任边界（没有内置沙箱）
