<a id="pi-agent-telemetry-schemas"></a>
# Pi Agent 遥测 Schema

<!-- Generated by generate-telemetry-docs.ts. Do not edit manually. -->

<a id="ai-request-schema"></a>
## AI 请求 schema

Schema 版本：1

<a id="piairequest"></a>
### `pi.ai.request`

对 AI 提供商的一次逻辑请求

- 父级：root 或任意调用方 span
- 默认状态：`ok`
- 出错条件：操作抛出或返回错误结果

<a id="start-attributes"></a>
#### 开始属性

| 名称 | 类型 | 必需 | 取值 | 备注 | 说明 |
|---|---|---:|---|---|---|
| `pi.ai.operation` | `string` | 是 | stream, fetch_deferred, cancel_deferred, generate_images |  | 逻辑提供商操作 |
| `pi.ai.provider` | `string` | 是 |  |  | 所选提供商 id |
| `pi.ai.model` | `string` | 是 |  |  | 请求的模型 id |
| `pi.ai.api` | `string` | 是 |  |  | 提供商 API id |
| `pi.ai.streaming` | `boolean` | 是 |  |  | 该操作是否返回流 |
| `pi.ai.deferred` | `boolean` | 否 |  |  | 该操作是否请求或参与延迟执行 |

<a id="end-attributes"></a>
#### 结束属性

所有结束属性都是可选的完成补充。

| 名称 | 类型 | 取值 | 备注 | 说明 |
|---|---|---|---|---|
| `pi.ai.response.model` | `string` |  |  | 具体响应模型 |
| `pi.ai.response.id` | `string` |  | 高基数 | 提供商响应 id |
| `pi.ai.response.stop_reason` | `string` | stop, length, tool_use, error, aborted, deferred |  | 归一化的终止响应原因 |
| `pi.ai.http.status_code` | `number` |  |  | 最终 HTTP 状态 |
| `pi.ai.usage.input_tokens` | `number` |  |  | 报告的 input tokens |
| `pi.ai.usage.output_tokens` | `number` |  |  | 报告的 output tokens |
| `pi.ai.usage.cache_read_tokens` | `number` |  |  | 报告的 cache-read tokens |
| `pi.ai.usage.cache_write_tokens` | `number` |  |  | 报告的 cache-write tokens |
| `pi.ai.usage.reasoning_tokens` | `number` |  |  | 报告的 reasoning tokens |
| `pi.ai.usage.total_tokens` | `number` |  |  | 报告的 total tokens |
| `pi.ai.usage.cost` | `number` |  |  | 报告的总费用 |
| `pi.ai.stream.chunk_count` | `number` |  |  | 流式更新分片数 |
| `pi.ai.stream.time_to_first_chunk_ms` | `number` |  |  | 到第一个更新分片的毫秒数 |
| `pi.ai.error.type` | `string` |  | 低基数 | 提供商或传输错误类 |

<a id="events"></a>
#### 事件

未声明 span 事件。

<a id="harness-schema"></a>
## Harness schema

Schema 版本：1

<a id="piharnessrun"></a>
### `pi.harness.run`

一次被接纳的进程内 run 调用

- 父级：root 或调用方自有的外部 span
- 默认状态：`ok`
- 出错条件：run 失败或抛出

<a id="start-attributes-1"></a>
#### 开始属性

| 名称 | 类型 | 必需 | 取值 | 备注 | 说明 |
|---|---|---:|---|---|---|
| `pi.session.id` | `string` | 是 |  | 高基数 | 会话 id |
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 是 |  | 高基数 | 持久操作 id |
| `pi.operation.recovery` | `boolean` | 是 |  |  | 该调用是否恢复持久工作 |
| `pi.operation.kind` | `string` | 是 | run |  | Run 操作种类 |

<a id="end-attributes-1"></a>
#### 结束属性

所有结束属性都是可选的完成补充。

| 名称 | 类型 | 取值 | 备注 | 说明 |
|---|---|---|---|---|
| `pi.operation.outcome` | `string` | completed, aborted, failed, suspended |  | Run 调用结果 |
| `pi.error.code` | `string` |  | 低基数 | 稳定的操作错误码 |
| `pi.error.type` | `string` |  | 低基数 | 低基数的操作错误类 |

<a id="events-1"></a>
#### 事件

未声明 span 事件。

<a id="piharnesscompaction"></a>
### `pi.harness.compaction`

一次被接纳的进程内手动压缩调用

- 父级：root 或调用方自有的外部 span
- 默认状态：`ok`
- 出错条件：压缩失败或抛出

<a id="start-attributes-2"></a>
#### 开始属性

| 名称 | 类型 | 必需 | 取值 | 备注 | 说明 |
|---|---|---:|---|---|---|
| `pi.session.id` | `string` | 是 |  | 高基数 | 会话 id |
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 是 |  | 高基数 | 持久操作 id |
| `pi.operation.recovery` | `boolean` | 是 |  |  | 该调用是否恢复持久工作 |
| `pi.operation.kind` | `string` | 是 | compaction |  | 压缩操作种类 |

<a id="end-attributes-2"></a>
#### 结束属性

所有结束属性都是可选的完成补充。

| 名称 | 类型 | 取值 | 备注 | 说明 |
|---|---|---|---|---|
| `pi.operation.outcome` | `string` | completed, declined, aborted, failed |  | 压缩调用结果 |
| `pi.error.code` | `string` |  | 低基数 | 稳定的操作错误码 |
| `pi.error.type` | `string` |  | 低基数 | 低基数的操作错误类 |

<a id="events-2"></a>
#### 事件

未声明 span 事件。

<a id="piharnessnavigation"></a>
### `pi.harness.navigation`

一次被接纳的进程内 navigation 调用

- 父级：root 或调用方自有的外部 span
- 默认状态：`ok`
- 出错条件：navigation 失败或抛出

<a id="start-attributes-3"></a>
#### 开始属性

| 名称 | 类型 | 必需 | 取值 | 备注 | 说明 |
|---|---|---:|---|---|---|
| `pi.session.id` | `string` | 是 |  | 高基数 | 会话 id |
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 是 |  | 高基数 | 持久操作 id |
| `pi.operation.recovery` | `boolean` | 是 |  |  | 该调用是否恢复持久工作 |
| `pi.operation.kind` | `string` | 是 | navigation |  | Navigation 操作种类 |

<a id="end-attributes-3"></a>
#### 结束属性

所有结束属性都是可选的完成补充。

| 名称 | 类型 | 取值 | 备注 | 说明 |
|---|---|---|---|---|
| `pi.operation.outcome` | `string` | completed, declined, aborted, failed |  | Navigation 调用结果 |
| `pi.error.code` | `string` |  | 低基数 | 稳定的操作错误码 |
| `pi.error.type` | `string` |  | 低基数 | 低基数的操作错误类 |

<a id="events-3"></a>
#### 事件

未声明 span 事件。

<a id="piharnesscheckpoint"></a>
### `pi.harness.checkpoint`

一次 run checkpoint

- 父级：`pi.harness.run`
- 默认状态：`ok`
- 出错条件：checkpoint 工作抛出

<a id="start-attributes-4"></a>
#### 开始属性

| 名称 | 类型 | 必需 | 取值 | 备注 | 说明 |
|---|---|---:|---|---|---|
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 是 |  | 高基数 | 持久操作 id |
| `pi.checkpoint.kind` | `string` | 是 | normal, failure_drain, abort_reconcile |  | Checkpoint 用途 |

<a id="end-attributes-4"></a>
#### 结束属性

所有结束属性都是可选的完成补充。

| 名称 | 类型 | 取值 | 备注 | 说明 |
|---|---|---|---|---|
| _none_ | | | | |

<a id="events-4"></a>
#### 事件

未声明 span 事件。

<a id="piharnessturn"></a>
### `pi.harness.turn`

一次 assistant 回复及其工具批次

- 父级：`pi.harness.run`
- 默认状态：`ok`
- 出错条件：回合工作抛出

<a id="start-attributes-5"></a>
#### 开始属性

| 名称 | 类型 | 必需 | 取值 | 备注 | 说明 |
|---|---|---:|---|---|---|
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 是 |  | 高基数 | 持久操作 id |
| `pi.turn.id` | `string` | 是 |  | 高基数 | 调用本地的回合 id |

<a id="end-attributes-5"></a>
#### 结束属性

所有结束属性都是可选的完成补充。

| 名称 | 类型 | 取值 | 备注 | 说明 |
|---|---|---|---|---|
| _none_ | | | | |

<a id="events-5"></a>
#### 事件

未声明 span 事件。

<a id="piharnessstep"></a>
### `pi.harness.step`

一次持久重试尝试

- 父级：`pi.harness.turn`、`pi.harness.checkpoint`、`pi.harness.compaction`、`pi.harness.navigation`
- 默认状态：`ok`
- 出错条件：该尝试重试、失败或抛出

<a id="start-attributes-6"></a>
#### 开始属性

| 名称 | 类型 | 必需 | 取值 | 备注 | 说明 |
|---|---|---:|---|---|---|
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 是 |  | 高基数 | 持久操作 id |
| `pi.step.kind` | `string` | 是 | assistant, compaction, branch_summary |  | 可重试的 step 种类 |
| `pi.step.attempt` | `number` | 是 |  |  | 从 1 开始的持久尝试次数 |
| `pi.compaction.reason` | `string` | 否 | manual, threshold, overflow |  | 压缩触发原因 |

<a id="end-attributes-6"></a>
#### 结束属性

所有结束属性都是可选的完成补充。

| 名称 | 类型 | 取值 | 备注 | 说明 |
|---|---|---|---|---|
| `pi.step.outcome` | `string` | succeeded, retry, failed, aborted, deferred, overflow |  | 尝试结果 |

<a id="events-6"></a>
#### 事件

未声明 span 事件。

<a id="piharnesstool"></a>
### `pi.harness.tool`

一次原始 phase-2 工具执行

- 父级：`pi.harness.turn`、`pi.harness.run`
- 默认状态：`ok`
- 出错条件：原始 phase-2 执行返回错误

<a id="start-attributes-7"></a>
#### 开始属性

| 名称 | 类型 | 必需 | 取值 | 备注 | 说明 |
|---|---|---:|---|---|---|
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 是 |  | 高基数 | 持久操作 id |
| `pi.turn.id` | `string` | 否 |  | 高基数 | 调用本地的实时回合 id |
| `pi.tool.name` | `string` | 是 |  |  | 工具名称 |
| `pi.tool.call_id` | `string` | 是 |  | 高基数 | 工具调用 id |
| `pi.tool.replay` | `string` | 是 | never, safe |  | 声明的回放策略 |
| `pi.tool.recovery` | `boolean` | 是 |  |  | 是否为恢复执行 |

<a id="end-attributes-7"></a>
#### 结束属性

所有结束属性都是可选的完成补充。

| 名称 | 类型 | 取值 | 备注 | 说明 |
|---|---|---|---|---|
| `pi.tool.is_error` | `boolean` |  |  | 原始 phase-2 执行是否返回错误 |

<a id="events-7"></a>
#### 事件

未声明 span 事件。

<a id="piharnesshook"></a>
### `pi.harness.hook`

一次已注册 hook handler 的调用

- 父级：root 或任意调用方 span
- 默认状态：`ok`
- 出错条件：handler 抛出

<a id="start-attributes-8"></a>
#### 开始属性

| 名称 | 类型 | 必需 | 取值 | 备注 | 说明 |
|---|---|---:|---|---|---|
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 否 |  | 高基数 | 被接纳时的持久操作 id |
| `pi.hook.name` | `string` | 是 | before_run, before_resume, before_run_end, transform_context, before_request, before_payload, after_response, before_tool, after_tool, before_compaction, before_navigation |  | Hook 名称 |
| `pi.hook.registration_id` | `string` | 否 |  |  | 稳定的 hook 注册 id |

<a id="end-attributes-8"></a>
#### 结束属性

所有结束属性都是可选的完成补充。

| 名称 | 类型 | 取值 | 备注 | 说明 |
|---|---|---|---|---|
| `pi.hook.outcome` | `string` | completed, skipped, blocked, failed |  | Handler 结果 |

<a id="events-8"></a>
#### 事件

未声明 span 事件。

<a id="piharnesssleep"></a>
### `pi.harness.sleep`

一次重试延迟

- 父级：`pi.harness.step`、`pi.harness.run`
- 默认状态：`ok`
- 出错条件：sleep 工作抛出

<a id="start-attributes-9"></a>
#### 开始属性

| 名称 | 类型 | 必需 | 取值 | 备注 | 说明 |
|---|---|---:|---|---|---|
| `pi.operation.id` | `string` | 是 |  | 高基数 | 持久操作 id |
| `pi.sleep.delay_ms` | `number` | 是 |  |  | 请求的延迟，单位毫秒 |

<a id="end-attributes-9"></a>
#### 结束属性

所有结束属性都是可选的完成补充。

| 名称 | 类型 | 取值 | 备注 | 说明 |
|---|---|---|---|---|
| `pi.sleep.outcome` | `string` | elapsed, aborted |  | 延迟结果 |

<a id="events-9"></a>
#### 事件

未声明 span 事件。

<a id="piharnessevent_handler"></a>
### `pi.harness.event_handler`

一次被动事件监听器调用

- 父级：root 或任意调用方 span
- 默认状态：`ok`
- 出错条件：监听器抛出

<a id="start-attributes-10"></a>
#### 开始属性

| 名称 | 类型 | 必需 | 取值 | 备注 | 说明 |
|---|---|---:|---|---|---|
| `pi.event.type` | `string` | 是 | run_start, run_resume, run_suspend, run_abort, run_end, fault, handler_error, turn_start, turn_end, retry_scheduled, retry_start, retry_end, message_start, message_update, message_end, tool_start, tool_update, tool_end, entry_added, write_pending, queue_update, fact_update, config_update, compaction_start, compaction_end, navigation_start, navigation_end, lane_created, usage | 低基数 | 投递的 harness 事件类型 |
| `pi.lane.name` | `string` | 否 |  | 高基数 | 作用域为 lane 的事件的 lane 名称 |

<a id="end-attributes-10"></a>
#### 结束属性

所有结束属性都是可选的完成补充。

| 名称 | 类型 | 取值 | 备注 | 说明 |
|---|---|---|---|---|
| _none_ | | | | |

<a id="events-10"></a>
#### 事件

未声明 span 事件。

<a id="pisessionwrite"></a>
### `pi.session.write`

一次已提交的会话变更

- 父级：root 或任意调用方 span
- 默认状态：`ok`
- 出错条件：存储拒绝该变更

<a id="start-attributes-11"></a>
#### 开始属性

| 名称 | 类型 | 必需 | 取值 | 备注 | 说明 |
|---|---|---:|---|---|---|
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 否 |  | 高基数 | 被接纳时的持久操作 id |
| `pi.session.mutation` | `string` | 是 | entry, record, lane, fact |  | 会话变更种类 |
| `pi.session.item_type` | `string` | 否 |  |  | Entry、record、lane 或 fact 子类型 |

<a id="end-attributes-11"></a>
#### 结束属性

所有结束属性都是可选的完成补充。

| 名称 | 类型 | 取值 | 备注 | 说明 |
|---|---|---|---|---|
| `pi.session.seq` | `number` |  |  | 暴露时的已提交会话序号 |

<a id="events-11"></a>
#### 事件

未声明 span 事件。
